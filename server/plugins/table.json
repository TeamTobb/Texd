{
  "tagname": "t",
  "html": "<div id='table_test'></div><script>var create_row = function(rowstack, text) { var split = text.split('|'); var curr_row = ''; for(var s in split) { curr_row += '<td>' + split[s] + '</td>'; } rowstack.push(curr_row); }; var elem = document.getElementById('table_test'); var str = '$value'; var rows = []; var curr_elements = ''; for(var i in str) { if(str[i] == '[') { continue; } else if(str[i] == ']') {create_row(rows, curr_elements); curr_elements = ''; } else { curr_elements += str[i]; } } var total = '<table>'; for (var row in rows) { total += '<tr>'; total += rows[row]; total += '</tr>'; } total += '</table>'; elem.innerHTML = total;</script>",
  "attr": [],
  "optattr": [],
  "description": "Table, example usage: '[1|2|3][4|5|6]'"
}